{
  "name": "food-court",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "killport": "npx kill-port 5000",
    "cleanup": "node cleanup.js",
    "dev": "npm run cleanup && concurrently \"npm run dev-frontend\" \"npm run dev-backend\"",
    "dev-frontend": "cd frontend && npm run dev",
    "dev-backend": "cd backend && npm run dev",
    "build": "cd frontend && npm install && npm run build",
    "start": "cd backend && npm install && node src/server.js",
    "fresh": "npm run cleanup && npm run dev"
  },
  "dependencies": {
    "concurrently": "^9.2.1"
  },
  "devDependencies": {
  }
}